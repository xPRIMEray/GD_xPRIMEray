# Telemetry Heatmap v2 — Sample

Short name: **Telemetry Heatmap v2 — Baseline Sample**

## Purpose

Sample run of the v2 heatmap system with the baseline pruning variant. Serves as a reference sample confirming the v2 heatmap format and metric set before committing to the full adaptive sweep pipeline.

## Source / Generation Context

- Scene: `test-curved-minimal-backdrop.tscn`
- Variant: baseline\_prune\_off, stride=1

## What the Output Shows

Eight heatmaps under the v2 format: same metrics as the adaptive baseline but generated by the v2 pipeline code. Confirms format compatibility.

## Key Files

- `*heat_resolve.png`, `*heat_d2k_max.png`, `*heat_query.png`
- `*heat_curvature_max.png`, `*heat_work_minus_curvature.png`
- `*telemetry_summary.json`

## Suggested MisterY Labs Card Summary

Interpretation pending — v2 heatmap pipeline sample run.

## Status

Archived

## Notes / Next Steps

- Superseded by the full adaptive sweep. Archive.
