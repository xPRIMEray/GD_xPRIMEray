# Shared Figure Captions

These captions are written in a common research-style voice so they can be reused directly across Papers 000–003 or adapted into a future unified manuscript.

## Figure A

Raw film-buffer capture from the deterministic wormhole harness. The image is saved directly from the film path without research-view composition, and therefore records the observer-facing render alone.

## Figure B

Composed deterministic validation capture showing the main film result together with the research overlay, portal-context inset, and compact contract status indicators.

## Figure C

Portal-local ring-density visualization derived from sector aggregation. The destination-side annulus highlighted in this figure is the ring used by the proto-caustic invariant.

## Figure D

Compact metrics card summarizing contract state, active low-value throttle profile, invariant-ring optical metrics, and the current pass-2 performance buckets for the deterministic harness.

## Figure E

Compact phase-space summary of the coupled invariant system. The figure distinguishes the under-constrained reference regime, the retained stable operating point, and the rejected stronger-throttle boundary.
